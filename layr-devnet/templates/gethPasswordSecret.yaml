apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.common.gethPassword.name }}
  namespace: {{ .Release.Namespace }}
type: Opaque
data:
{{- range $key, $value := .Values.common.gethPassword.values }}
  {{ $key }}: {{ $value | b64enc }}
{{- end }}
