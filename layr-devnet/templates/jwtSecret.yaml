apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.common.jwtSecret.name }}
  namespace: {{ .Release.Namespace }}
type: Opaque
data:
{{- range $key, $value := .Values.common.jwtSecret.values }}
  {{ $key }}: {{ $value | b64enc }}
{{- end }}
